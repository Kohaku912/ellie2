# Ellie の自己改善リクエスト
AI が「今すぐ直したい」と判断した改善だけを、短い自然文で残すメモです。

## 保留中の改善
- 自律実行や自己呼び出しの開始時に、`overlay_show` だけでなく開始通知の overlay を必ず出す。
- `request_user_approval` は、PC サーバー側の二択 overlay prompt を使って Yes / No を返せるようにする。
- PC ツールは静的登録に頼らず、接続してきた PC サーバーのツールレジストリをそのまま使う。
- 指示や改善文の例は増やしすぎず、同じ方向に偏りにくい簡潔な文に整える。
